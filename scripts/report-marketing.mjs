import { readFile, writeFile, stat } from 'node:fs/promises';
import { resolve } from 'node:path';
import ts from 'typescript';
import sharp from 'sharp';

const output='artifacts/public-website';
const report=JSON.parse(await readFile(`${output}/seo-browser-report.json`,'utf8'));
const performance=JSON.parse(await readFile(`${output}/performance.json`,'utf8'));
const source=await readFile('src/content/marketing/pages.ts','utf8');
const compiled=ts.transpileModule(source,{compilerOptions:{module:ts.ModuleKind.ESNext,target:ts.ScriptTarget.ES2022}}).outputText;
const {marketingPages}=await import(`data:text/javascript;base64,${Buffer.from(compiled).toString('base64')}`);
const fileLink=(label,file)=>`[${label}](<${resolve(file).replaceAll('\\','/')}>)`;
const assets=await Promise.all(['dashboard','dashboard-mobile','amazon','product','cogs','groups','copilot','sync'].map(async name=>({name,bytes:(await stat(`public/marketing/${name}.webp`)).size})));
for(const [input,name,height] of [['homepage-desktop','homepage-hero',1000],['homepage-390','mobile-homepage',1000]]){
  const file=`${output}/browser/${input}.png`;const meta=await sharp(file).metadata();
  await sharp(file).extract({left:0,top:0,width:meta.width,height:Math.min(height,meta.height)}).png().toFile(`${output}/${name}.png`);
}
const routes=[{slug:'',eyebrow:'Homepage',keywords:['marketplace profitability software','ecommerce profit analytics','multi marketplace analytics']},...marketingPages];
const routeTable=routes.map(p=>`| /${p.slug} | ${p.eyebrow} | ${p.keywords.join('; ')} |`).join('\n');
const sections=[
 ['Implementation summary', 'Implemented the complete public website within the existing Next.js, React, TypeScript and Tailwind project. Fourteen indexable pages, a branded public 404, shared marketing components, product imagery, access CTAs and a local-only contact experience are included.'],
 ['Public route map', `All public pages are prerendered. The catch-all page uses an explicit registry and generateStaticParams; unknown public paths return 404.\n\n| Route | Topic | Search intent |\n|---|---|---|\n${routeTable}`],
 ['Marketing layout architecture', 'app/(marketing)/layout.tsx supplies MarketingHeader, a skip link, one main landmark and MarketingFooter. It never renders TenantShell. RuntimeBoundary dynamically loads the existing AppProviders only for /auth, /onboarding, /o and /platform, preserving one provider instance across those private-route transitions.'],
 ['Component system', 'MarketingHeader, MarketingFooter, Brand, MarketingCTA, ProductScreenshot, SEOPage, Eyebrow, CheckList, FAQ, CTASection, FinancialWaterfall, CostHistory, GroupExample, JsonLd and ContactForm form the reusable public component system. Existing Radix dropdown and dialog primitives provide keyboard menus and the mobile focus trap.'],
 ['Homepage structure', 'Hero → factual marketplace strip → financial explanation → combined channels → Product profitability → COGS workflow → historical costs → Product Groups → Copilot → freshness → multi-company access → onboarding → module capabilities → team responsibilities → planned platform direction → FAQ → final CTA → footer. Editorial rows, a full-width product frame, diagrams and selected contrasting sections provide variation.'],
 ['Hero copy', '“Marketplace profitability you can actually explain.” Supporting copy: “Revenue is only the beginning. Understand product costs, fees, advertising, refunds and net profit across Amazon, eBay and Temu.”'],
 ['Positioning statement', 'Stock Supplies helps marketplace businesses connect what sold with what it cost and what remains. Explainability, effective-dated costs and visible completeness support the financial narrative.'],
 ['CTA strategy', 'The consistent primary label is Start Test Plan, matching existing onboarding terminology and linking to /auth/register. Sign in links to /auth/sign-in. Product exploration and Talk to us are contextual secondary actions. Stable data-cta attributes support future instrumentation; no external tracker is installed.'],
 ['Marketplace Profitability page', 'Covers Revenue → Refunds → Net Revenue → COGS → Fees → Advertising → Shipping → Other Costs → Net Profit; shared financial context, period comparison, incomplete costs and investigation into Products. Includes the actual Dashboard and a labelled financial example.'],
 ['Product Profitability page', 'Explains internal Product versus Marketplace Listing, units and unit economics, multi-marketplace comparison, low-margin and loss-making Products, trends, historical cost and direct versus inherited cost sources. It does not advertise inventory or purchasing as current functionality.'],
 ['COGS page', 'Substantial cost-specific content covers CSV/XLSX/Paste, bulk and single edits, mapping and matching, anomalies, approval, audit, historical effective dates and backdated corrections. An actual import mapping crop and an accessible cost-history diagram support the workflow.'],
 ['Product Groups page', 'Uses Disposable Gloves: 1,000 gloves at £100, producing £2.50/£5.00/£10.00 for 25/50/100 packs; a £110 basis produces £2.75/£5.50/£11.00. Effective dates, membership history and direct Product overrides are explained. This is costing, not inventory packaging or BOM functionality.'],
 ['Copilot page', 'Contextual profitability explanations, spreadsheet interpretation, Product matches, anomalies and Product Group suggestions are described. The page and homepage visibly state that financial changes require authorised human approval.'],
 ['Amazon page', 'Focuses on revenue, orders, refunds, fees, advertising and owned Product costs, with a real Amazon-filtered Dashboard. Account context and data freshness qualify the result. No unsupported Amazon API, partner or live-production claims are made.'],
 ['eBay page', 'Focuses on sales, fees, refunds, Product identity and costing, account filtering and authentication/sync visibility. Includes related links to financial explanation, Products and COGS.'],
 ['Temu page', 'Focuses on Product/account profitability, applicable costs and combined marketplace context. It explicitly avoids claiming advanced Temu advertising or guaranteed production API datasets.'],
 ['Pricing page', 'Explains Module 01 inclusions, Test Plan access and the modular platform. Commercial pricing remains subject to operational testing and operating-cost analysis. No invented plan tiers, monthly prices, free-tier promises or structured offers appear.'],
 ['Security page', 'Describes roles, assignments, tenant context, human financial approval, audit and separate privileged administration. It distinguishes the demonstrated access model from production security assurance and claims no certification or uptime guarantee.'],
 ['About page', 'Conservative product positioning around connected commerce operations and a focused first module. No founder story, customer history, dates, endorsements or case studies are fabricated.'],
 ['Contact page', 'Includes Name, Work email, Company, Role, Marketplaces, account count, Message and optional enquiry interest. The page clearly discloses local browser storage and no email delivery.'],
 ['Content architecture', 'src/content/marketing/pages.ts defines typed records with slug, title, description, eyebrow, headline, intro, screenshot, sections, workflow, FAQ, related links and keyword intent. src/content/marketing/seo.ts owns URL, metadata and structured-data construction. The homepage has a dedicated editorial composition and shared FAQ source.'],
 ['Screenshot strategy', 'Real approved app captures are cropped without changing Product values or inventing controls. Fresh captures cover the combined Dashboard, Amazon-filtered Dashboard, Product Detail, Copilot explanation and Dashboard freshness. Existing COGS mapping and Product Group evidence is curated. The mobile hero uses a fresh actual mobile KPI region. Source manifests remain under artifacts, not public navigation.'],
 ['Responsive behavior', 'Reviewed at 1440px, 1280px, 834px tablet and 390 × 844 mobile. Mobile uses a modal navigation drawer, stacked content, reflowed financial diagrams and a focused KPI image. Other wide Product screenshots retain legibility in labelled, keyboard-focusable horizontal regions. No page-level horizontal overflow was found.'],
 ['Accessibility work', 'One H1 and main landmark per public route, skip link, visible focus, semantic links and buttons, native FAQ details/summary, Radix keyboard menus, mobile focus trapping and Escape restoration, image alternatives, required form labels, live success/error states and reduced-motion handling. Drawer navigation, FAQ keyboard operation and focus behavior are covered by browser assertions. This is not an external accessibility certification.'],
 ['SEO keyword map', 'The route table above is the keyword-intent map. Feature pages address their own commercial questions; Amazon, eBay and Temu have marketplace-specific content rather than duplicated homepage copy. There are no thin blog, calculator or competitor pages.'],
 ['Metadata implementation', 'Every public route has a unique title and description, matching Open Graph title/description, an OG image, Twitter summary_large_image metadata and an indexable robots value. Metadata and the primary H1 are present in production server HTML.'],
 ['Canonical implementation', 'Canonicals use the trusted NEXT_PUBLIC_SITE_URL origin and explicit clean public paths. Query parameters do not enter canonicals. Set NEXT_PUBLIC_SITE_URL to the verified production HTTPS origin before a public deployment, then rebuild; the local environment is currently the canonical configuration.'],
 ['Sitemap implementation', '/sitemap.xml is generated from the explicit public registry and contains exactly the fourteen indexable public URLs. Auth, onboarding, tenant workspaces, platform administration and development routes are excluded.'],
 ['Robots implementation', '/robots.txt permits public content and disallows private paths. Private layouts emit noindex, nofollow metadata; next.config.ts also emits X-Robots-Tag headers for auth, onboarding, o and platform routes. These are indexing controls, not authentication.'],
 ['Structured data', 'Homepage Organization and WebApplication; feature/integration WebApplication where appropriate; BreadcrumbList on inner pages; FAQPage derived from the same visibly rendered FAQ records. Validation checks shape, positions and matching answer text. No review, rating or price offers are fabricated.'],
 ['Internal linking', 'Feature relationships are embedded in related-link blocks, header/footer navigation and the homepage narrative. Marketplace hub links lead to Amazon, eBay and Temu; cost pages link to Groups and Product profitability. The validator resolves internal URL targets and checks anchored sections.'],
 ['Image optimization', `Eight WebP product assets total ${Math.round(assets.reduce((sum,item)=>sum+item.bytes,0)/1024)} KiB, ranging from ${Math.round(Math.min(...assets.map(a=>a.bytes))/1024)} to ${Math.round(Math.max(...assets.map(a=>a.bytes))/1024)} KiB. Statically imported assets receive content-hashed URLs and fixed dimensions. Next Image serves these already-optimized files without a redundant transformation. Only the device-appropriate primary hero asset is preloaded; below-fold assets are lazy. The branded 1200 × 630 OG image uses the existing mark and actual Dashboard crop.`],
 ['Core Web Vitals and performance work', `The final local Chrome sample recorded CLS ${performance.metrics.cls}, LCP ${Math.round(performance.metrics.lcp)} ms and ${performance.metrics.longTasks} observed long tasks. This is a local, synthetic sample, not a field or Lighthouse score. Browser caching was disabled for the final sample. The public layout does not load the authenticated provider/query runtime, and no marketing chart or animation library was added.`],
 ['Conversion flow', 'Primary registration and distinct sign-in links use the existing approved routes. Browser QA activates both links and checks that the existing forms render. Mobile menu navigation changes the route and closes the drawer. No duplicate public signup page was introduced.'],
 ['Contact form behavior', 'HTML constraints and explicit validation cover the required values and marketplace selection. Submission shows a brief saving state, then stores one enquiry locally. Storage rejection shows a recoverable error with entries intact; copying is available where browser clipboard access permits. No HTTP request or email submission is performed. Local test enquiries are removed by the QA script.'],
 ['Public 404', 'Unknown public paths return HTTP 404 with Stock Supplies branding, Return home and Explore Module 01 links. app/o/not-found.tsx preserves the former authenticated record-not-found presentation within the private subtree.'],
 ['SEO validator results', `${report.summary.passed} assertions passed; ${report.summary.failed} failed. Checks cover rendered pages, HTTP responses, metadata, canonical paths, sitemap and robots, schema/FAQ correspondence, links, headings, image alternatives, CTAs, prohibited claims, focus behavior and responsive overflow. See seo-browser-report.json and validation-log.txt.`],
 ['Build and static verification', 'A clean production build with Next.js webpack completed successfully and prerendered all fourteen marketing pages. TypeScript and ESLint passed. The existing Foundation, Onboarding, Analytics, Products, COGS and Product Groups validators all passed. A Windows process interruption required regeneration of the build cache; no source rollback was performed. See build-log.txt and static-checks.txt.'],
 ['Browser QA', `${report.evidence.length} screenshots accompany the route and interaction checks. Browser QA covers every requested public page, 404, footer targets, account entry, mobile menu focus and navigation, contact success/failure and the requested desktop/tablet/mobile widths. ${report.runtimeErrors.length} runtime exceptions were recorded in the final run.`],
 ['Lighthouse/performance findings', 'Lighthouse is not installed in the supplied project/runtime, so no Lighthouse score is claimed. Local Chrome PerformanceObserver metrics and resource details are saved in performance.json. INP needs representative interaction/field sampling and is not asserted as a production result. Fixed image geometry, native disclosures, limited client islands and compact local assets reduce avoidable layout and interaction work.'],
 ['Known limitations', 'This is a local implementation, not a published production deployment. Final commercial terms, verified production domain, operational marketplace connections, production security assurance and real contact delivery remain outside this frontend phase. The dedicated authenticated Sync Health route is a foundation screen; public visual evidence uses implemented Dashboard freshness/coverage and clearly labelled illustrative statuses. No legal policy, testimonials or certifications were fabricated.'],
 ['Authenticated-app files changed and why', 'app/layout.tsx now uses the lightweight route runtime boundary instead of unconditionally loading AppProviders. app/auth/layout.tsx, app/onboarding/layout.tsx and app/platform/layout.tsx add only robots metadata. New app/o/layout.tsx adds private robots metadata; app/o/not-found.tsx preserves the former record 404. next.config.ts adds indexing headers. No authenticated screen, financial calculation, role, repository or Phase 1–5 workflow was edited. The working tree already contained extensive earlier-phase changes; these were preserved.'],
 ['Phase 6 boundary', 'No Phase 6 Transactions implementation was started. Existing transaction routes and files were not edited.'],
 ['Future-module boundary', 'Inventory, purchasing, sales operations, warehousing, fulfilment, finance, communications, automation, portal and workforce expansion are labelled planned. None is marketed as an available operational module.'],
];
const markdown=`# Stock Supplies public website completion report\n\nGenerated ${new Date().toISOString().slice(0,10)}.\n\n${fileLink('Visual evidence index',`${output}/visual-evidence.md`)} · ${fileLink('Machine-readable QA report',`${output}/seo-browser-report.json`)}\n\n${sections.map(([title,body],i)=>`## ${i+1}. ${title}\n\n${body}`).join('\n\n')}\n`;
await writeFile(`${output}/completion-report.md`,markdown);
await writeFile(`${output}/visual-evidence.md`,`# Stock Supplies visual evidence\n\nFinal production-build captures. All financial and company examples are illustrative product data.\n\n${fileLink('Homepage hero',`${output}/homepage-hero.png`)} · ${fileLink('Mobile homepage',`${output}/mobile-homepage.png`)}\n\n${report.evidence.map((item,i)=>`${i+1}. ${fileLink(item.name,item.file)}`).join('\n')}\n`);
const cards=report.evidence.map(item=>`<a href="browser/${item.name}.png"><img src="browser/${item.name}.png" alt="${item.name.replaceAll('-',' ')}" loading="lazy"><span>${item.name.replaceAll('-',' ')}</span></a>`).join('');
await writeFile(`${output}/visual-evidence.html`,`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Stock Supplies — Visual Evidence</title><style>body{margin:0;padding:40px;font:16px/1.6 system-ui;color:#14243a;background:#f4f7fb}h1{letter-spacing:-1px}main{display:grid;grid-template-columns:repeat(auto-fit,minmax(260px,1fr));gap:24px}a{display:block;border:1px solid #ccd8e9;border-radius:6px;overflow:hidden;background:white;text-decoration:none;color:inherit}img{display:block;width:100%;height:240px;object-fit:cover;object-position:top;border-bottom:1px solid #e2e9f1}span{display:block;padding:15px;text-transform:capitalize}p{color:#597087}</style><h1>Stock Supplies / Public website</h1><p>Production-build visual evidence. Select any capture to open its full resolution.</p><main>${cards}</main></html>`);
console.log(`Completion report and ${report.evidence.length}-image evidence gallery written.`);
