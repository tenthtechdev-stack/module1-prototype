# Show the review controller in local and deployed builds; false hides it.
# Netlify reads this public flag at build time: redeploy after changing it.
NEXT_PUBLIC_PROTOTYPE_MODE=true
NEXT_PUBLIC_SITE_URL=http://localhost:3000
